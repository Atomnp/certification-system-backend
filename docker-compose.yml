services:
  app:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - /var/database/locus_certification_system:/app/db
      - /var/www/static:/app/static

  nginx:
    build: ./nginx
    ports:
      - "80:80"
    depends_on:
      - app
    volumes:
      - /var/www/static:/static